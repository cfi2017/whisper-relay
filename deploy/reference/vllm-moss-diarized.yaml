apiVersion: apps/v1
kind: Deployment
metadata:
  name: moss-diarized-asr
  namespace: vllm
  labels:
    app: moss-diarized-asr
spec:
  replicas: 1
  progressDeadlineSeconds: 3600
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: moss-diarized-asr
  template:
    metadata:
      labels:
        app: moss-diarized-asr
    spec:
      terminationGracePeriodSeconds: 60
      nodeSelector:
        kubernetes.io/arch: arm64
      volumes:
        - name: cache-volume
          persistentVolumeClaim:
            claimName: hf-cache
        - name: shm
          emptyDir:
            medium: Memory
            sizeLimit: 8Gi
      containers:
        - name: vllm
          image: vllm/vllm-openai:latest-audio
          imagePullPolicy: IfNotPresent
          command: ["/bin/sh", "-c"]
          args:
            - |
              exec vllm serve OpenMOSS-Team/MOSS-Transcribe-Diarize \
                --served-model-name moss-diarized \
                --trust-remote-code \
                --dtype bfloat16 \
                --gpu-memory-utilization 0.20 \
                --max-model-len 65536 \
                --max-num-seqs 2 \
                --host 0.0.0.0 \
                --port 8000
          env:
            - name: HF_HOME
              value: /root/.cache/huggingface
            - name: HF_HUB_CACHE
              value: /root/.cache/huggingface/hub
            - name: VLLM_MAX_AUDIO_CLIP_FILESIZE_MB
              value: "512"
            - name: NVIDIA_VISIBLE_DEVICES
              value: all
            - name: NVIDIA_DRIVER_CAPABILITIES
              value: compute,utility
          ports:
            - name: http
              containerPort: 8000
          resources:
            requests:
              cpu: "4"
              memory: 12Gi
              nvidia.com/gpu: "1"
            limits:
              cpu: "12"
              memory: 32Gi
              nvidia.com/gpu: "1"
          volumeMounts:
            - name: cache-volume
              mountPath: /root/.cache/huggingface
            - name: shm
              mountPath: /dev/shm
          startupProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 400
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: moss-diarized-asr
  namespace: vllm
spec:
  selector:
    app: moss-diarized-asr
  ports:
    - name: http
      port: 8000
      targetPort: http
  type: ClusterIP
